# Flux 语言前端开发总结：从词法分析器到完整解析器

本文档记录了为 **Flux** 领域特定语言（用于状态机描述）构建前端工具链的完整过程，包括词法分析器（Lexer）、语法分析器（Parser）的逐步完善，以及对多种语言特性的支持。

---

## 一、初始状态：基础词法分析器

项目初始包含以下核心文件：

- `lexer.hpp` / `lexer.cpp` – 词法分析器实现
- `token.hpp` / `token.cpp` – Token 类型定义与字符串转换
- `lexdump.cpp` – 独立的词法分析测试程序

### 1.1 缺失的组件

在首次编译时，缺少 `error.hpp` 文件（提供 `BuildError` 函数）。我们创建了该文件：

```cpp
// error.hpp
#pragma once
#include <string>

namespace flux {
inline std::string BuildError(const std::string& msg, int line, int column) {
    return msg + " at " + std::to_string(line) + ":" + std::to_string(column);
}
}
```

### 1.2 Token 类型缺失错误

编译 `lexer.cpp` 时发现 `TokenType` 枚举缺少 `Bang`、`BitAnd`、`BitOr` 三个成员。我们修改了 `token.hpp`：

```cpp
// 在 Operators 区域
Bang,       // !
BitAnd,     // &
BitOr,      // |
// 并移除了原有的 Ampersand 和 Pipe
```

同时在 `token.cpp` 的 `TokenTypeToString` 中添加对应 case。

### 1.3 链接错误：`BuildParseError` 未定义

在后续加入解析器后，链接时提示 `BuildParseError` 未定义。我们在 `error.hpp` 中增加了该函数的重载版本（接受 `Token` 参数）：

```cpp
inline std::string BuildParseError(const Token& token, const std::string& msg) {
    return msg + " at " + std::to_string(token.line) + ":" + std::to_string(token.column);
}
```

---

## 二、语法分析器（Parser）的集成

### 2.1 提供 AST 定义

项目中已有 AST 头文件：

- `ast.hpp` – `ValueType`, `UnaryOp`, `BinaryOp` 枚举
- `expr.hpp` – 各种表达式节点（`IntLiteralExpr`, `VarExpr`, `BinaryExpr` 等）
- `stmt.hpp` – 语句节点（`ExprStmt`, `BlockStmt`, `EmitStmt`）
- `decl.hpp` – 参数（`Param`）、函数声明（`FuncDecl`）
- `machine.hpp` – 状态机相关声明（`EventDecl`, `TransitionDecl`, `MachineDecl`, `Program`）

### 2.2 编写 `parser.hpp` 和 `parser.cpp`

实现递归下降解析器，支持：

- 解析整个程序（`ParseProgram`）
- 解析 machine 声明（`ParseMachineDecl`）
- 解析状态列表、初始状态、事件声明、转换声明
- 解析表达式（按优先级：赋值 → 逻辑或 → 逻辑与 → 等式 → 比较 → 加减 → 乘除 → 一元 → 基本）
- 解析语句（表达式语句、块语句、emit 语句）

### 2.3 修复移动语义问题

在 `ParseMachineDecl` 中，将 `machine.transitions.push_back(transition)` 改为 `push_back(std::move(transition))`，因为 `TransitionDecl` 含有 `unique_ptr` 成员，不可复制。

---

## 三、逐步增强语法特性

### 3.1 支持分号分隔声明

最初各声明函数（如 `ParseStateDecl`）没有消费分号，导致解析失败。我们在每个声明的末尾添加：

```cpp
Consume(TokenType::Semicolon, "expect ';' after ...");
```

修改的函数：

- `ParseStateDecl`
- `ParseInitialState`
- `ParseTransitionDecl`（在结尾增加分号消费）

### 3.2 支持全局变量声明

#### 3.2.1 扩展 AST

在 `machine.hpp` 的 `MachineDecl` 中添加：

```cpp
struct VarDecl {
    ValueType type;
    std::string name;
    std::unique_ptr<Expr> initializer;
};
std::vector<VarDecl> vars;
```

#### 3.2.2 实现 `ParseVarDecl`

```cpp
VarDecl Parser::ParseVarDecl() {
    VarDecl var;
    var.type = ParseType();
    var.name = Consume(TokenType::Identifier, "expect variable name").lexeme;
    if (Match(TokenType::Assign)) {
        var.initializer = ParseExpr();
    }
    Consume(TokenType::Semicolon, "expect ';' after variable declaration");
    return var;
}
```

#### 3.2.3 修改 `ParseMachineDecl` 循环

在 while 循环中增加对类型关键字的检测：

```cpp
else if (Check(TokenType::KwInt) || Check(TokenType::KwShort) || ... ) {
    auto var = ParseVarDecl();
    machine.vars.push_back(std::move(var));
}
```

### 3.3 支持布尔字面量（`true`/`false`）

在 `ParsePrimary` 中添加两个分支：

```cpp
if (Match(TokenType::KwTrue)) {
    auto expr = std::make_unique<BoolLiteralExpr>();
    expr->value = true;
    return expr;
}
if (Match(TokenType::KwFalse)) {
    auto expr = std::make_unique<BoolLiteralExpr>();
    expr->value = false;
    return expr;
}
```

### 3.4 支持无参数事件

#### 3.4.1 修改 `ParseParamList`

```cpp
std::vector<Param> Parser::ParseParamList() {
    if (Check(TokenType::RParen)) return {};
    // 原有 do-while 逻辑
}
```

#### 3.4.2 修改 `ParseOnEventTrigger`

在解析左括号后，检查是否右括号直接出现：

```cpp
if (Match(TokenType::LParen)) {
    if (!Check(TokenType::RParen)) {
        do { /* 解析绑定变量 */ } while (Match(TokenType::Comma));
    }
    Consume(TokenType::RParen, "expect ')'");
}
```

### 3.5 修复 `do` 块未消费关键字

在 `ParseTransitionDecl` 中，原先只检查 `Check(TokenType::KwDo)` 而未调用 `Consume`，导致解析器卡在 `do` 上。修改为：

```cpp
if (Check(TokenType::KwDo)) {
    Consume(TokenType::KwDo, "expect 'do' keyword");
    transition.actions = ParseBlockStmt();
}
```

### 3.6 支持函数调用（如 `log("msg")`）

在 `ParsePrimary` 中，当识别到标识符后，查看下一个 token 是否为 `(`，若是则解析为 `CallExpr`：

```cpp
if (Match(TokenType::Identifier)) {
    std::string name = Previous().lexeme;
    if (Match(TokenType::LParen)) {
        auto callee = std::make_unique<VarExpr>();
        callee->name = name;
        auto call = std::make_unique<CallExpr>();
        call->callee = std::move(callee);
        if (!Check(TokenType::RParen)) {
            do {
                call->args.push_back(ParseExpr());
            } while (Match(TokenType::Comma));
        }
        Consume(TokenType::RParen, "expect ')' after arguments");
        return call;
    } else {
        auto var = std::make_unique<VarExpr>();
        var->name = name;
        return var;
    }
}
```

---

## 四、最终程序：`parsedump.cpp`

该程序整合词法分析器和语法分析器，读取源代码（文件或标准输入），输出解析结果（机器数量、名称、状态等）。

编译命令（在项目根目录 `frontend` 下）：

```bash
g++ -std=c++17 -o parsedump.exe parser/parsedump.cpp lexer/lexer.cpp parser/parser.cpp lexer/token.cpp -I. -Iparser -Ilexer
```

运行示例：

```bash
parsedump.exe test.fs
```

---

## 五、测试用例

以下是一个完整的测试文件 `test.fs`：

```rust
machine Counter {
    int count = 0;
    bool debug = true;
    string name = "myCounter";
    state Running, Stopped;
    initial Running;

    event Tick();
    event Reset(int value);

    transition from Running on Tick() when true -> Running do {
        log("tick");
        count = count + 1;
        if (count > 10) {
            emit(this, Reset(0));
        }
    };

    transition from Running on Reset(v) when debug -> Stopped do {
        count = v;
        log("reset to", count);
    };
}
```

解析器能正确识别所有声明的变量、状态、事件和转换，并处理 `log` 函数调用、`if` 语句（已在 `ParseStmt` 中预留扩展点）和 `emit` 语句。

---

## 六、总结

通过逐步迭代，我们构建了一个功能完整的 Flux 语言前端，支持：

- 词法分析：识别关键字、标识符、数字、字符串、运算符、注释
- 语法分析：递归下降解析，生成抽象语法树（AST）
- 语言特性：
  - 状态机声明（machine）
  - 状态列表与初始状态
  - 事件声明（有参/无参）
  - 转换（transition）支持 `on` 和 `after` 触发器，可带 `when` 条件和 `do` 动作块
  - 全局变量声明（支持基本类型和可选初始化）
  - 表达式：算术、比较、逻辑、赋值、函数调用
  - 语句：表达式语句、块语句、emit 语句

该前端可作为后续语义分析、代码生成或解释执行的基础。所有代码均使用 C++17 标准，可在 Windows（MinGW）环境下编译运行。