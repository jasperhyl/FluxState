摘要：
在runtime下增加了main.c 入口函数
在runtime下增加了instrument函数，用于给LLVM IR新增输出函数调用
编译指令：
$ clang++ -std=c++17 instrument.cpp -o instrument.exe `llvm-config --cxxflags --ldflags --libs --system-libs` -lpthread
$ ./instrument.exe test.ll instrumented.ll 
$ clang -c -o instrumented.o instrumented.ll

总编译运行指令：
$ clang -c -o main.o main.c
$ clang -c -o runtime.o runtime.c -std=c99 -D_POSIX_C_SOURCE=200809L
$ clang -o program main.o test.o/instrumented.o runtime.o -lwinpthread
$ ./program
注意：所有编译指令必须在linux/MSYS2 CLANG64下运行！

# FluxState 运行时与代码插桩工具使用说明

本文档介绍如何编译运行 FluxState 生成的状态机 IR 代码，以及如何使用 `instrument` 工具在 LLVM IR 中插入函数调用日志，便于调试和跟踪。

## 目录结构（示例）

```
runtime/
├── main.c              # 程序入口，调用状态机初始化与运行
├── runtime.c           # 状态机运行时库（事件队列、定时器、状态调度）
├── runtime.h           # 运行时头文件
├── instrument.cpp      # LLVM IR 插桩工具：在每个函数调用前插入 printf
├── test.ll             # 由 FluxState 前端生成的原始 LLVM IR
└── instrumented.ll     # 插桩后生成的 LLVM IR
```

## 1. 入口函数 `main.c`

`main.c` 负责初始化状态机运行时，并启动状态机的事件循环。

```c
#include "runtime.h"

// 由 IR 文件导出的函数声明
extern void fs_module_init(void);
extern void fs_run_SyntaxCoverage(void);   // 示例状态机，名称按实际修改

int main(void) {
    fs_module_init();          // 初始化所有状态机（调用 fs_init_xxx）
    fs_run_SyntaxCoverage();   // 运行指定状态机（阻塞式事件循环）
    return 0;
}
```

> 如果有多个状态机，可以依次调用各自的 `fs_run_xxx`，或自行实现调度逻辑。

## 2. 插桩工具 `instrument.cpp`

该工具使用 LLVM 库读取 LLVM IR 文件，在每个函数调用点之前插入 `printf("Call function: %s\n", 函数名)`，从而在运行时打印每次函数调用的名称，便于跟踪执行流。

### 编译插桩工具

在 **MSYS2 CLANG64** 或 **Linux** 环境下执行：

```bash
clang++ -std=c++17 instrument.cpp -o instrument.exe `llvm-config --cxxflags --ldflags --libs --system-libs` -lpthread
```

> 如果 `llvm-config` 不在 PATH 中，请使用完整路径（例如 `/clang64/bin/llvm-config`）。

### 使用插桩工具

```bash
./instrument.exe test.ll instrumented.ll
```

- `test.ll` ：原始 LLVM IR 文件
- `instrumented.ll` ：输出文件，包含插桩后的 IR

## 3. 完整编译与运行流程

### 3.1 编译各模块

```bash
# 编译插桩后的 IR 为目标文件（如果不需要插桩，可直接编译原始 test.ll）
clang -c -o instrumented.o instrumented.ll

# 编译入口函数
clang -c -o main.o main.c

# 编译运行时库（POSIX 版本）
clang -c -o runtime.o runtime.c -std=c99 -D_POSIX_C_SOURCE=200809L
```

### 3.2 链接生成可执行文件

```bash
clang -o program main.o instrumented.o runtime.o -lwinpthread
```

> 在 **Linux** 环境下，链接命令可能需要调整为：
> ```bash
> clang -o program main.o instrumented.o runtime.o -lpthread -lrt
> ```

### 3.3 运行

```bash
./program
```

运行后，程序会打印类似如下的函数调用日志：

```
Call function: fs_init_SyntaxCoverage
Call function: fs_action_SyntaxCoverage_0
Call function: fs_enqueue_event
...
```

## 4. 注意事项

1. **环境要求**  
   - 所有编译命令必须在 **MSYS2 CLANG64** 或 **Linux** 环境下执行（因为使用了 POSIX 函数 `clock_gettime`, `nanosleep` 和 `printf`）。
   - 在 Windows 原生环境（如 MSVC）下无法直接编译运行。

2. **运行时库链接**  
   - MSYS2 CLANG64 下需要链接 `-lwinpthread` 以提供 `clock_gettime` 等函数。
   - Linux 下需要链接 `-lpthread -lrt`。

3. **插桩工具依赖**  
   - 需要安装 LLVM 开发库（`llvm-config` 可用）。在 MSYS2 中执行：
     ```bash
     pacman -S mingw-w64-clang-x86_64-llvm
     ```

4. **函数名获取**  
   - 插桩工具只支持直接调用（`call` 指令），对间接调用（通过函数指针）会打印 `"indirect"` 或 `"unknown"`。

5. **性能影响**  
   - 插桩会显著增加输出量和运行时间，仅建议用于调试阶段。生产环境请使用原始 IR。

## 5. 一键构建脚本（示例）

将以下内容保存为 `build.sh`，赋予执行权限后运行：

```bash
#!/bin/bash
set -e

# 1. 插桩（可选）
if [ -f "test.ll" ]; then
    ./instrument.exe test.ll instrumented.ll
    IR_TO_COMPILE="instrumented.ll"
else
    echo "test.ll not found, abort."
    exit 1
fi

# 2. 编译
clang -c -o ${IR_TO_COMPILE%.ll}.o ${IR_TO_COMPILE}
clang -c -o main.o main.c
clang -c -o runtime.o runtime.c -std=c99 -D_POSIX_C_SOURCE=200809L

# 3. 链接
clang -o program main.o ${IR_TO_COMPILE%.ll}.o runtime.o -lwinpthread

# 4. 运行
./program
```

## 6. 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `undefined reference to clock_gettime` | 缺少实时库 | Linux 添加 `-lrt`，MSYS2 添加 `-lwinpthread` |
| `llvm-config: command not found` | 未安装 LLVM 或未设置 PATH | 安装 LLVM 开发包，或使用完整路径 |
| 插桩后运行无输出 | `printf` 未刷新缓冲区 | 在格式字符串中加入 `\n`，或调用 `fflush(stdout)` |
| 状态机不执行任何动作 | 没有事件入队或定时器触发 | 检查原始 IR 中是否有初始事件或定时器启动 |
