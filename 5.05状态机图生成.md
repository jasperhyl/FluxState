摘要：
新增状态机图生成模块（Visualizer），支持从 FluxState 源码自动生成 DOT（Graphviz）和 Mermaid 两种格式的状态转换图。
新增文件：backend/visualizer/visualizer.hpp、backend/visualizer/visualizer.cpp
修改文件：frontend/main.cpp（新增 --dot / --mermaid 命令行参数）、build_ir.sh（加入 visualizer.cpp 编译）

# FluxState 状态机图生成功能

## 功能说明

编译器新增 `--dot` 和 `--mermaid` 两个命令行选项，可在编译管线中 Lowering 阶段之后直接输出状态机的状态转换图，无需进入 IRGen 阶段。

**编译管线变化：**

```
源码(.fs) → Lexer → Parser → Sema → Lowering → ┬→ IRGen（默认，生成 LLVM IR）
                                                  ├→ --dot（生成 DOT/Graphviz 图）
                                                  └→ --mermaid（生成 Mermaid 图）
```

## 新增/修改文件

```
backend/visualizer/
├── visualizer.hpp      # Visualizer 类声明
└── visualizer.cpp      # ToDot() + ToMermaid() 实现

frontend/main.cpp       # 新增 --dot / --mermaid 命令行参数解析
build_ir.sh             # 编译列表加入 backend/visualizer/visualizer.cpp
```

## 使用方法

### 编译编译器

```bash
bash build_ir.sh
```

### 生成 DOT 格式（Graphviz）

```bash
./fluxstate_ir --dot test/test.fs > test/test.dot
```

输出示例：
```dot
digraph SyntaxCoverage {
    rankdir=LR;
    node [shape=circle, style=filled, fillcolor=white, fontname="Arial"];
    edge [fontname="Arial", fontsize=10];

    __initial [shape=point, width=0, style=invis];
    __initial -> Idle [label=""];

    Idle [fillcolor="#4CAF50", fontcolor=white];
    Armed;
    Waiting;
    ...

    Idle -> Armed [label="on Ping()"];
    Armed -> Waiting [label="on Configure(code, retry, ...) [when ...]"];
    Waiting -> Cooling [label="after 5s [when ...]"];
    ...
}
```

### 生成 Mermaid 格式

```bash
./fluxstate_ir --mermaid test/test.fs > test/test.mmd
```

输出示例：
```
stateDiagram-v2
    direction LR
    [*] --> Idle
    Idle --> Armed : on Ping()
    Armed --> Waiting : on Configure(code, retry, ...) [when ...]
    Waiting --> Cooling : after 5s [when ...]
    ...
```

### 渲染为图片（用于 PPT）

```bash
# 安装 Graphviz
apt install -y graphviz

# 渲染为 PNG
dot -Tpng test/test.dot -o test/test.png

# 渲染为 SVG（矢量图，更清晰）
dot -Tsvg test/test.dot -o test/test.svg
```

### 在线查看 Mermaid

将 .mmd 文件内容粘贴到 https://mermaid.live ，即可实时预览并导出 PNG/SVG。

## 图元素说明

| 元素 | 含义 |
|------|------|
| 绿色填充节点 | 初始状态（`initial` 声明的状态） |
| 白色节点 | 普通状态 |
| `on EventName(params)` | 事件触发的转换 |
| `after Xs` | 超时触发的转换 |
| `[when ...]` | 该转换带有 guard 守卫条件 |
| 空心箭头 `__initial → State` | 标记初始状态的入口 |

## 实现原理

Visualizer 遍历 `LoweredProgram` 中每个 `LoweredMachine` 的：
- `states` — 获取所有状态节点
- `initial_state_tag` — 标记初始状态
- `transitions` — 对每条转换，通过 `src_state_tag` / `dst_state_tag` 查找状态名，通过 `trigger_kind` / `trigger_id` 查找事件名或定时器描述，生成带标签的有向边
