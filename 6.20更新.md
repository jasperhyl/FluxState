# 6.20 更新

## 一、Docker 支持

新增 Docker 化构建环境，基于 **Ubuntu 22.04 + LLVM 19**，无需本地安装 LLVM 工具链。

### 新增文件

- `Dockerfile`：LLVM 19 镜像，自动构建 `fluxstate_ir`
- `.dockerignore`：排除 `.git`、构建产物

### Dockerfile 关键处理

编译期通过 `sed` 修复以下兼容问题（不修改本地源文件）：

| 问题 | 修复 |
|------|------|
| 源文件 Windows CRLF 换行 | `sed -i 's/\r$//'` |
| `llvm::Triple` 无隐式 `StringRef` 转换 | `Triple(...)` → `Triple(...).str()` 或直接传字符串 |
| `CC_BIN` 需要绝对路径 | `CC_BIN=/usr/local/bin/clang` |
| 创建无版本号 symlink | `clang-19` → `clang`，`clang++-19` → `clang++` |

### 使用方式

```bash
docker build -t fluxstate .
docker run -it fluxstate

# 挂载输出目录
docker run --rm -v "${PWD}/test/present/out:/workspace/test/present/out" fluxstate ./test/present/run_demo.sh
```

---

## 二、`--debug` 选项（状态机插桩）

为 `fluxstate_ir --emit-exe` 新增 `--debug` 选项，可在运行时打印完整的状态迁移轨迹。

### 插桩层级

| 层级 | 输出内容 | 输出示例 |
|------|---------|---------|
| Runner（C） | 函数入口 | `main` / `inject_initial_events` |
| IR（LLVM） | 状态迁移 | `[CrossingController] VehicleGreen -> VehicleYellow` |

### 实现方式

- **Runner 层**（[executable_builder.cpp](file:///e:/作业/编译2/FluxState/backend/linker/executable_builder.cpp)）：生成的 C runner 函数入口插入 `system("echo ...")`，仅 `main` 和 `inject_initial_events`（`runner_now_ms` / `runner_sleep_ms` 因高频调用被排除）
- **IR 层**（[machine_irgen.cpp](file:///e:/作业/编译2/FluxState/backend/irgen/machine_irgen.cpp)）：在 `EmitExecuteTransition` 中调用 `puts("[machine] src -> dst")`，每条 transition 触发时输出一行
- 使用 `CreateGlobalString` 声明全局字符串常量（替代 LLVM 19 中已移除的 `CreateGlobalStringPtr`）
- `RuntimeDecls` 新增 `puts_fn`，编译时声明 `puts` 外部函数

### 涉及文件

| 文件 | 变更 |
|------|------|
| `backend/irgen/irgen_context.hpp` | `RuntimeDecls` 新增 `puts_fn`；新增 `_debug` 成员 + `SetDebug`/`Debug` 方法 |
| `backend/irgen/irgen.hpp` | `Generate()` 新增 `bool debug = false` 参数 |
| `backend/irgen/irgen.cpp` | 透传 `debug` |
| `backend/irgen/runtime_decls.cpp` | 声明 `puts` 外部函数 |
| `backend/irgen/machine_irgen.hpp` | 新增 `SetDebug()` + `_debug` 成员 |
| `backend/irgen/machine_irgen.cpp` | 在 `EmitExecuteTransition` 中插桩 |
| `backend/linker/executable_builder.hpp` | `ExecutableBuildOptions` 新增 `bool debug = false` |
| `backend/linker/executable_builder.cpp` | 生成的 runner C 代码中插桩 |
| `frontend/main.cpp` | 解析 `--debug` 参数，更新 usage 文本 |

### 使用示例

```bash
./fluxstate_ir --emit-exe /tmp/program --debug test/test.fs
/tmp/program
```

输出：

```text
main
inject_initial_events
[CrossingController] VehicleGreen -> VehicleYellow
[DisplayPanel] Cars -> Waiting
[CrossingController] VehicleYellow -> PedWalk
[DisplayPanel] Waiting -> Walk
[CrossingController] PedWalk -> PedFlash
[DisplayPanel] Walk -> Flashing
[CrossingController] PedFlash -> VehicleGreen
[DisplayPanel] Flashing -> Cars
```

---

## 三、测试脚本更新

| 文件 | 变更 |
|------|------|
| `test/run_tests.sh` | 两处 `--emit-exe` 新增 `--debug` |
| `test/present/run_demo.sh` | 新增 `--debug`；`--idle-timeout-ms` 50→2000，`--max-runtime-ms` 4000→6000（smart_crossing 的 `after 1s` timer 需要足够长的空闲超时） |

---

## 四、文档更新

| 文件 | 变更 |
|------|------|
| `README.md` | 新增 Docker 环境说明；新增 `--debug` 选项章节（含输出示例） |
| `test/present/README.md` | 新增 Docker 运行方式；`--debug` 输出示例；更新 `--idle-timeout-ms` / `--max-runtime-ms` 值；修正"当前演示限制"描述 |
| `6.20更新.md` | 本文档 |

---

## 五、文件清单

### 新增

- `Dockerfile`
- `.dockerignore`
- `6.20更新.md`

### 修改

- `backend/irgen/irgen_context.hpp`
- `backend/irgen/irgen.hpp`
- `backend/irgen/irgen.cpp`
- `backend/irgen/runtime_decls.cpp`
- `backend/irgen/machine_irgen.hpp`
- `backend/irgen/machine_irgen.cpp`
- `backend/linker/executable_builder.hpp`
- `backend/linker/executable_builder.cpp`
- `frontend/main.cpp`
- `test/run_tests.sh`
- `test/present/run_demo.sh`
- `README.md`
- `test/present/README.md`
