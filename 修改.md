# 修改记录（Lexer 修复 + 驱动程序）

本文记录：修复 `frontend/lexer/lexer.cpp` 的关键 bug，并新增一个最小驱动程序用于验证 token 输出。

---

## 1. 修复内容概览

### 1.1 修复的核心问题

1) `Match()` 只判断、不消费字符

- **原问题**：`Match(expected)` 返回 true 时没有推进 `_current/_column`，导致 `==`, `!=`, `->`, `++`, `--`, `<=`, `>=`, `&&`, `||` 等二字符 token 只消费了第一个字符。
- **修复**：匹配成功时调用 `Advance()` 消费该字符，再返回 true。

2) 标识符扫描条件写反，几乎会吞掉所有字符

- **原问题**：`LexIdentifierOrKeyword()` 里使用 `isalnum(c) || c != '_'`，只要 `c` 不是 `_` 就成立，导致标识符读取不会在符号处停止。
- **修复**：改为 `isalnum(c) || c == '_'`。

3) 字符串扫描使用 `PeekNext()`，会漏读/错读

- **原问题**：`LexString()` 里用 `PeekNext() != '"'` 控制循环，容易错过最后一个字符并造成关闭引号处理错误。
- **修复**：改为 `Peek() != '"'`，并明确 `Advance()` 消费右引号。

4) 行注释扫描使用 `PeekNext()`，行为不稳定

- **原问题**：`//...` 注释扫描使用 `PeekNext() != '\n'`，会在行末尾出现边界行为问题。
- **修复**：改为 `Peek() != '\n'`，一直消费到换行或 EOF。

5) 块注释未闭合不抛异常

- **原问题**：未闭合块注释时只构造了 `std::runtime_error(...)` 对象但没有 `throw`。
- **修复**：改为 `throw std::runtime_error(...)`。

6) `&&` token 类型生成错误

- **原问题**：在 `case '&'` 分支里，`&&` 被错误地生成成 `TokenType::OrOr`。
- **修复**：改为 `TokenType::AndAnd`。

7) `initial` 关键字未加入关键字表

- **原问题**：`token.hpp` 定义了 `KwInitial`，但 `KeywordMap` 没有 `"initial"` 映射。
- **修复**：补充 `{"initial", TokenType::KwInitial}`。

### 1.2 调试可用性改进

1) `TokenTypeToString()` 覆盖更多 token

- **改进**：为 `BitAnd`/`BitOr`/`Increment`/`Decrement` 增加映射，避免打印时出现 `<unknown-token>`。

2) `BuildError()` 报错格式更易读

- **改进**：`"Lexer: Error at"` 后增加空格，变为 `"Lexer: Error at 12:3: ..."`。

---

## 2. 新增：最小验证驱动程序 lexdump

新增文件：`frontend/lexer/lexdump.cpp`

功能：

- `lexdump.exe <path>`：读取指定文件并打印 tokens。直接运行，默认打印lexdump.cpp中的例子

输出格式（每行一个 token）：

- `line:column TokenType "lexeme"`

其中 `lexeme` 会做转义显示（\n, \t, \" 等），便于观察字符串内容。

---

## 3. 涉及文件列表

- 修复：`frontend/lexer/lexer.cpp`
- 改进：`frontend/lexer/token.cpp`
- 改进：`frontend/lexer/error.cpp`
- 新增：`frontend/lexer/lexdump.cpp`

